export default function Settings() {
  return (
    <section className="page-card">
      <h2>Settings</h2>

      <p>
        Future settings can include clinic profile, user login, payment plan,
        report preferences, and multi-clinic data separation.
      </p>
    </section>
  );
}